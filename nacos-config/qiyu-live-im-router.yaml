spring:
  application:
    name: jiushi-live-im-router-provider
  data:
    redis:
      port: 6379
      host: 192.168.1.20
      password: mylovezq
      lettuce:
        pool:
          min-idle: 10
          max-active: 100
          max-idle: 10
qiyu:
  rmq:
    producer:
      nameSrv: 192.168.1.20:9876
      groupName: ${spring.application.name}
      applicationName: ${spring.application.name}
      sendMsgTimeout: 3000
      retryTimes: 2

dubbo:
  application:
    name: ${spring.application.name}
    qos-enable: false
  registry:
    address: nacos://192.168.1.20:8848?namespace=jiushiLive&&username=qiyu&&password=qiyu
  protocol:
    port: 9098
